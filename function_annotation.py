from typing import List

def function_name(param1: type, param2: type) -> return_type:

    pass

# sum

# math.ceil

# math.floor

# Function that takes a name and outputs a message

# Function that takes two floats and finds their average.

# Function that takes and age and returns true if they are of voting age

def sum(numbers: List[float]) -> float:
    """Adds up all numbers in a list.

    Args:
        numbers: A list of numbers.
    
    Returns:
        The sum of the numbers in a list.
    """
    pass

def ceil(num: float) -> int:
    """Round a decimal number to the nearest greater integer.

    Args:
        A decimal number.

    Returns:
        The number rounded up.
    """
    pass 

def floor(num: float) -> int:
    """Round a decimal number down to the nearest integer.

    Args:
        num: Decimal number to round down.
    
    Returns: The number rounded down.
    """
    pass

#Function that takes a name and returns a message
def create_message(name: str) -> str:
    """Returns a personalized message returning somebodys name

    Args: 
        name: Someone's name
    
    Returns: 
        A personalized message including someones name.
    """
    pass
#Function that takes two floatrs and finds their average.
def avg(a: float, b: float) -> float:
    """Gets the average of two numbers.

    Args:
        a: A number
        b: Another number
    
    Returns:
        The average of the two numbers.
    """
    pass

def can_vote(age: int) -> bool:
    """Determines wheter or not you can vote

    Args: 
        age: the age of some person

    Returns:
        True if they can vote, False otherwise.
    """
    pass